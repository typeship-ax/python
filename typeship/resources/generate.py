"""typeship — generate.

Generated by typeship — https://typeship.dev
"""
from __future__ import annotations

from typing import Any, Dict, List, Literal, Optional, Union

from .._core import HttpCore, RequestOptions
from ..models import *  # noqa: F401,F403 — generated payload types


class GenerateResource:
    def __init__(self, core: HttpCore) -> None:
        self._core = core

    def run(
        self,
        *,
        body: GenerateRequest,
        idempotency_key: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> GenerationResultRead:
        """Generate a package from a Spec

        Returns one generated package without creating a Project.

        Supports [idempotent retries](https://typeship.dev/docs/typeship-api/idempotency); keyed
        responses include generated files in the replay cache.

        Use `download.url` to save the complete ZIP, verify `download.sha256`, and extract it
        into an empty directory. The link expires at `download.expires_at` and grants access to
        anyone who has it. CLI, MCP, and SDK calls supply an idempotency key automatically.
        Agents should request `fields=["download","coverage","warnings","claim"]` to keep the
        MCP result compact; files can exceed the response limit. Download the ZIP instead of
        repeating generation to retrieve omitted files.

        Anonymous and Free requests include the first 25 operations. Paid plans include all
        operations. Anonymous requests are rate limited by IP address. Check `coverage` for
        omitted operations; an invalid API key returns `401`.

        An anonymous URL request without source headers may return `claim.url`. Sign in through
        that link within seven days to save the recipe as a Project.

        POST /generate

        Args:
            idempotency_key: Identifies one logical write for 24 hours. The key is
                scoped to the authenticated organization and operation; generation
                without an organization uses a hashed network identity. Retrying the
                same method, path, query, If-Match header, and JSON body replays the
                original response. Reusing the key with changed intent returns 409.
                After expiry the key starts a new write.
        """
        _headers = {
            "Idempotency-Key": idempotency_key,
        }
        _errors = {
            "400": "BadRequestError",
            "401": "UnauthorizedError",
            "403": "ForbiddenError",
            "409": "ConflictError",
            "413": "PayloadTooLargeError",
            "422": "UnprocessableEntityError",
            "429": "RateLimitedError",
            "500": "InternalServerError",
            "default": "ApiResponseError",
        }
        return self._core.request(
            "POST",
            "/generate",
            headers=_headers,
            body=body,
            errors=_errors,
            idempotency_key_header="Idempotency-Key",
            security=[{},{"apiKey":[]}],
            request_options=request_options,
            schema_key="generate.run",
        )

    def download_package(
        self,
        *,
        token: str,
        request_options: Optional[RequestOptions] = None,
    ) -> bytes:
        """Download a generated package

        Download the complete ZIP referenced by `generate_run`'s `download.url`. Pass the token
        from that URL. No API key is needed; the token grants access only to that exact package
        until its replay window expires. Keep the token private.

        The local MCP server saves this binary response to disk. On a hosted MCP connection,
        download the original URL directly to your workspace. Verify the ZIP against
        `download.sha256` before extracting it into an empty directory. Expired or invalid
        tokens return `404`; a new generation creates a new download.

        GET /generate/download

        Args:
            token: Private download token from download.url in the generation result.
        """
        _query = {
            "token": token,
        }
        _errors = {
            "400": "BadRequestError",
            "404": "NotFoundError",
            "429": "RateLimitedError",
            "500": "InternalServerError",
        }
        return self._core.request(
            "GET",
            "/generate/download",
            query=_query,
            errors=_errors,
            idempotent=True,
            security=[{}],
            request_options=request_options,
            schema_key="generate.downloadPackage",
        )


class AsyncGenerateResource:
    def __init__(self, core: HttpCore) -> None:
        self._core = core

    async def run(
        self,
        *,
        body: GenerateRequest,
        idempotency_key: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> GenerationResultRead:
        """Generate a package from a Spec

        Returns one generated package without creating a Project.

        Supports [idempotent retries](https://typeship.dev/docs/typeship-api/idempotency); keyed
        responses include generated files in the replay cache.

        Use `download.url` to save the complete ZIP, verify `download.sha256`, and extract it
        into an empty directory. The link expires at `download.expires_at` and grants access to
        anyone who has it. CLI, MCP, and SDK calls supply an idempotency key automatically.
        Agents should request `fields=["download","coverage","warnings","claim"]` to keep the
        MCP result compact; files can exceed the response limit. Download the ZIP instead of
        repeating generation to retrieve omitted files.

        Anonymous and Free requests include the first 25 operations. Paid plans include all
        operations. Anonymous requests are rate limited by IP address. Check `coverage` for
        omitted operations; an invalid API key returns `401`.

        An anonymous URL request without source headers may return `claim.url`. Sign in through
        that link within seven days to save the recipe as a Project.

        POST /generate

        Args:
            idempotency_key: Identifies one logical write for 24 hours. The key is
                scoped to the authenticated organization and operation; generation
                without an organization uses a hashed network identity. Retrying the
                same method, path, query, If-Match header, and JSON body replays the
                original response. Reusing the key with changed intent returns 409.
                After expiry the key starts a new write.
        """
        _headers = {
            "Idempotency-Key": idempotency_key,
        }
        _errors = {
            "400": "BadRequestError",
            "401": "UnauthorizedError",
            "403": "ForbiddenError",
            "409": "ConflictError",
            "413": "PayloadTooLargeError",
            "422": "UnprocessableEntityError",
            "429": "RateLimitedError",
            "500": "InternalServerError",
            "default": "ApiResponseError",
        }
        return await self._core.arequest(
            "POST",
            "/generate",
            headers=_headers,
            body=body,
            errors=_errors,
            idempotency_key_header="Idempotency-Key",
            security=[{},{"apiKey":[]}],
            request_options=request_options,
            schema_key="generate.run",
        )

    async def download_package(
        self,
        *,
        token: str,
        request_options: Optional[RequestOptions] = None,
    ) -> bytes:
        """Download a generated package

        Download the complete ZIP referenced by `generate_run`'s `download.url`. Pass the token
        from that URL. No API key is needed; the token grants access only to that exact package
        until its replay window expires. Keep the token private.

        The local MCP server saves this binary response to disk. On a hosted MCP connection,
        download the original URL directly to your workspace. Verify the ZIP against
        `download.sha256` before extracting it into an empty directory. Expired or invalid
        tokens return `404`; a new generation creates a new download.

        GET /generate/download

        Args:
            token: Private download token from download.url in the generation result.
        """
        _query = {
            "token": token,
        }
        _errors = {
            "400": "BadRequestError",
            "404": "NotFoundError",
            "429": "RateLimitedError",
            "500": "InternalServerError",
        }
        return await self._core.arequest(
            "GET",
            "/generate/download",
            query=_query,
            errors=_errors,
            idempotent=True,
            security=[{}],
            request_options=request_options,
            schema_key="generate.downloadPackage",
        )
