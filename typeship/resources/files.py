"""typeship — files.

Generated by typeship — https://typeship.dev
"""
from __future__ import annotations

from typing import Any, Dict, List, Literal, Optional, Union
from urllib.parse import quote as _quote

from .._core import HttpCore, RequestOptions
from ..models import *  # noqa: F401,F403 — generated payload types


class FilesResource:
    def __init__(self, core: HttpCore) -> None:
        self._core = core

    def get(
        self,
        file_id: FileId,
        *,
        cursor: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> FileResponseRead:
        """Get a file

        Returns one bounded chunk of an immutable file: at most 24 KiB, as UTF-8 text or, for
        binary bytes, base64. When next_cursor is not null, repeat the request with cursor and
        concatenate the chunks in order. A file ID always returns the same bytes.

        GET /files/{file_id}

        Args:
            cursor: next_cursor from the preceding chunk of this file.
        """
        _query = {
            "cursor": cursor,
        }
        _errors = {
            "400": "BadRequestError",
            "401": "UnauthorizedError",
            "403": "ForbiddenError",
            "404": "NotFoundError",
            "429": "RateLimitedError",
            "500": "InternalServerError",
        }
        return self._core.request(
            "GET",
            f"/files/{_quote(str(file_id), safe='')}",
            query=_query,
            errors=_errors,
            idempotent=True,
            security=[{"apiKey":[]}],
            request_options=request_options,
            schema_key="files.get",
        )


class AsyncFilesResource:
    def __init__(self, core: HttpCore) -> None:
        self._core = core

    async def get(
        self,
        file_id: FileId,
        *,
        cursor: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> FileResponseRead:
        """Get a file

        Returns one bounded chunk of an immutable file: at most 24 KiB, as UTF-8 text or, for
        binary bytes, base64. When next_cursor is not null, repeat the request with cursor and
        concatenate the chunks in order. A file ID always returns the same bytes.

        GET /files/{file_id}

        Args:
            cursor: next_cursor from the preceding chunk of this file.
        """
        _query = {
            "cursor": cursor,
        }
        _errors = {
            "400": "BadRequestError",
            "401": "UnauthorizedError",
            "403": "ForbiddenError",
            "404": "NotFoundError",
            "429": "RateLimitedError",
            "500": "InternalServerError",
        }
        return await self._core.arequest(
            "GET",
            f"/files/{_quote(str(file_id), safe='')}",
            query=_query,
            errors=_errors,
            idempotent=True,
            security=[{"apiKey":[]}],
            request_options=request_options,
            schema_key="files.get",
        )
