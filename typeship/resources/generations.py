"""typeship — generations.

Generated by typeship — https://typeship.dev
"""
from __future__ import annotations

from typing import Any, Dict, List, Literal, Optional, Union
import asyncio
import time
from urllib.parse import quote as _quote

from .._core import HttpCore, RequestOptions
from ..models import *  # noqa: F401,F403 — generated payload types


class GenerationsResource:
    def __init__(self, core: HttpCore) -> None:
        self._core = core

    def retrieve(
        self,
        generation_id: GenerationId,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> GenerationResponseRead:
        """Retrieve a generation

        Returns the current Generation status. `queued` and `running` mean generation is still
        in progress. `succeeded` means generated files are saved, not that repository delivery
        or a Draft is complete. Successful results include files, or a file index when the
        package is too large to inline.

        GET /generations/{generation_id}
        """
        _errors = {
            "401": "UnauthorizedError",
            "403": "ForbiddenError",
            "404": "NotFoundError",
            "429": "RateLimitedError",
            "500": "InternalServerError",
        }
        return self._core.request(
            "GET",
            f"/generations/{_quote(str(generation_id), safe='')}",
            errors=_errors,
            idempotent=True,
            security=[{"apiKey":[]}],
            request_options=request_options,
            schema_key="generations.retrieve",
        )

    def retrieve_file(
        self,
        generation_id: GenerationId,
        *,
        path: str,
        request_options: Optional[RequestOptions] = None,
    ) -> str:
        """Fetch one file from a generation

        Returns one file's raw content. Use a path from `files_index` when the Generation
        reports `files_omitted: true`.

        GET /generations/{generation_id}/file

        Args:
            path: Repo-relative path inside the generated package.
        """
        _query = {
            "path": path,
        }
        _errors = {
            "400": "BadRequestError",
            "401": "UnauthorizedError",
            "403": "ForbiddenError",
            "404": "NotFoundError",
            "429": "RateLimitedError",
            "500": "InternalServerError",
        }
        return self._core.request(
            "GET",
            f"/generations/{_quote(str(generation_id), safe='')}/file",
            query=_query,
            errors=_errors,
            idempotent=True,
            security=[{"apiKey":[]}],
            request_options=request_options,
            schema_key="generations.retrieveFile",
        )

    def wait(self, generation_id: GenerationId, *, interval: float = 1.0,
             timeout: float = 600.0, request_options: Optional[RequestOptions] = None) -> GenerationResponseRead:
        """Poll until generated files are saved or the Generation fails."""
        deadline = time.monotonic() + timeout
        while True:
            result = self.retrieve(generation_id, request_options=request_options)
            if result["status"] not in ("queued", "running"):
                return result
            remaining = deadline - time.monotonic()
            if remaining <= 0:
                raise TimeoutError(f"Timed out waiting for Generation {generation_id}.")
            time.sleep(min(interval, remaining))


class AsyncGenerationsResource:
    def __init__(self, core: HttpCore) -> None:
        self._core = core

    async def retrieve(
        self,
        generation_id: GenerationId,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> GenerationResponseRead:
        """Retrieve a generation

        Returns the current Generation status. `queued` and `running` mean generation is still
        in progress. `succeeded` means generated files are saved, not that repository delivery
        or a Draft is complete. Successful results include files, or a file index when the
        package is too large to inline.

        GET /generations/{generation_id}
        """
        _errors = {
            "401": "UnauthorizedError",
            "403": "ForbiddenError",
            "404": "NotFoundError",
            "429": "RateLimitedError",
            "500": "InternalServerError",
        }
        return await self._core.arequest(
            "GET",
            f"/generations/{_quote(str(generation_id), safe='')}",
            errors=_errors,
            idempotent=True,
            security=[{"apiKey":[]}],
            request_options=request_options,
            schema_key="generations.retrieve",
        )

    async def retrieve_file(
        self,
        generation_id: GenerationId,
        *,
        path: str,
        request_options: Optional[RequestOptions] = None,
    ) -> str:
        """Fetch one file from a generation

        Returns one file's raw content. Use a path from `files_index` when the Generation
        reports `files_omitted: true`.

        GET /generations/{generation_id}/file

        Args:
            path: Repo-relative path inside the generated package.
        """
        _query = {
            "path": path,
        }
        _errors = {
            "400": "BadRequestError",
            "401": "UnauthorizedError",
            "403": "ForbiddenError",
            "404": "NotFoundError",
            "429": "RateLimitedError",
            "500": "InternalServerError",
        }
        return await self._core.arequest(
            "GET",
            f"/generations/{_quote(str(generation_id), safe='')}/file",
            query=_query,
            errors=_errors,
            idempotent=True,
            security=[{"apiKey":[]}],
            request_options=request_options,
            schema_key="generations.retrieveFile",
        )

    async def wait(self, generation_id: GenerationId, *, interval: float = 1.0,
                   timeout: float = 600.0, request_options: Optional[RequestOptions] = None) -> GenerationResponseRead:
        """Poll until generated files are saved or the Generation fails."""
        deadline = time.monotonic() + timeout
        while True:
            result = await self.retrieve(generation_id, request_options=request_options)
            if result["status"] not in ("queued", "running"):
                return result
            remaining = deadline - time.monotonic()
            if remaining <= 0:
                raise TimeoutError(f"Timed out waiting for Generation {generation_id}.")
            await asyncio.sleep(min(interval, remaining))
